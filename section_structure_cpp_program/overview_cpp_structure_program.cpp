//
// Created by foobar on 5/21/2020.
//

//C++ has 90 keywords, which is quite a lot.

#include <iostream>

int overwiev(){

    int number;

    std::cout << "Enter something";
    // << stream insert operator, not a keyword
    // >> stream write operator, not a keyword
    // Basically make up for the syntax, [; " ' << >>] things that are not keywords
    return 0;
}